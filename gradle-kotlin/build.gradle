buildscript {
    ext.kotlin_version = '{{kotlin.version}}'

    repositories {
        jcenter()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"

        {{gradle-dependencies}}
    }
}

apply plugin: 'kotlin'

